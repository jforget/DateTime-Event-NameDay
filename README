This module handles generating name days from dates and vice versa.

INSTALLATION

To install this module type the following in the distribution
directory:

   perl Makefile.PL
   make
   make test
   make install

or use a tool like the CPAN shell or CPAN-Minus (cpanm).
